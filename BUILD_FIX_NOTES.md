# LocalVault V1.5 数据迁移 / 恢复改进

本版本基于 `LocalVault_V1_5_DataTransferSecurityFix` 修改。

## 已修改

1. **恢复 Vault 备份增加两种模式**
   - **合并到当前 Vault**：保留当前 Vault 的现有/新增条目；备份中当前没有的条目追加，并自动重新编号；缺少的分类会加入。
   - **替换当前 Vault**：保持原来的完整恢复逻辑，用备份快照替换当前 Vault。
   - 恢复前仍要求输入备份对应 Vault 的主密码并验证，验证失败不会替换当前数据库。
   - 合并时不会直接覆盖当前 Vault 的数据库文件，也不会覆盖当前 Vault 的恢复设置。

2. **密保答案取消“至少 12 个字符”限制**
   - 现在只要求答案不能为空。
   - 支持中文、英文、数字及混合答案。
   - 中文不再被迫填写 12 个汉字。
   - Recovery Code 本身仍保持独立的长度要求。

3. **界面提示同步更新**
   - 恢复备份按钮说明改为“可选择合并或替换”。
   - 密保答案输入框改为“不能为空，可使用中文”。
   - 数据迁移文档补充了合并恢复行为和密保答案规则。

## 验证

- 已完成 TypeScript/TSX 语法级转译检查。
- 已完成修改文件的括号/花括号平衡检查。
- 当前执行环境没有 Cargo/Rust 工具链，因此无法在这里执行 Windows Tauri 的 `cargo check` / `npm.cmd run build`；请在 Windows 项目目录执行：

```powershell
npm.cmd install
npm.cmd run build
npm.cmd run tauri:dev
```



## V1.6.2 主密码规则统一

- 所有原来的“至少 12 个字符”主密码限制统一为“至少 8 位”。
- 新建主密码、找回主密码后的新密码、主界面“密保及密码修改”中的新主密码，统一要求：至少 8 位 + 数字 + 大写字母 + 小写字母 + 特殊符号。
- 修改主密码时，新密码不得与旧主密码相同。
- 找回主密码时，即使没有旧密码输入框，后端也会通过当前 Vault 的旧密钥封装值检测新密码是否与旧密码相同。
- 主密码验证、完整备份导出、账号密码导入等涉及既有密码的校验最低长度统一为 8 位，不再出现 12 位限制。
