# LocalVault 功能介绍

> 本文档基于源码逐项整理 LocalVault 1.7.8 的全部功能，按「安全体系 → 密码管理 → 分类 → 数据管理 → 系统功能 → 发布形态」分类说明。

---

## 一、安全体系

### 1.1 强加密存储
- 使用 **Argon2id**（128 MiB 内存、3 迭代、2 并行）对主密码进行密钥派生；
- 使用 **XChaCha20-Poly1305** 认证加密（AEAD）加密所有数据；
- 每条密码记录使用独立的 24 字节随机 nonce，并绑定记录 ID 作为附加认证数据（AAD），密文被篡改或挪用时无法解密；
- 内存中的密钥在锁定、退出时通过 `zeroize` 即时清零。

### 1.2 主密码策略
- 创建 Vault 时强制主密码**至少 8 位**，且必须同时包含**数字、大写字母、小写字母、特殊符号**；
- 主密码不落盘，仅用于派生解密密钥；
- 提供**密码强度实时评分条**（0–100 分），并对 `password/123456` 等常见弱密码、连续重复字符、键盘顺序等进行扣分。

### 1.3 找回主密码（Recovery Code + 密保）
- 创建 Vault 后即可设置**恢复方式**：一组离线 **Recovery Code**（分组大写字母数字，如 `ABCDEF-GHIJKL-...`）+ **三组本地密保问答**；
- 忘记主密码时，可用「Recovery Code + 三组答案」验证身份后**重新设置新主密码**；
- 修改密保会**自动生成新的 Recovery Code**，旧码立即失效；
- Recovery Code 支持一键复制到剪贴板，建议离线保存。

### 1.4 自动锁定
- 空闲自动锁定：可选 **1 / 5 / 10 / 30 分钟**无操作后自动锁定 Vault；
- 支持手动「🔒 锁定」按钮，立即清空内存数据并回到解锁页。

### 1.5 剪贴板保护
- 复制密码后 **30 秒自动清空剪贴板**，减少密码在剪贴板中被他人读取的风险。

### 1.6 Windows 文件保护
- 自动为 Vault 数据目录设置**隐藏 + 只读**属性；
- 每次启动都按「已锁定」处理并恢复该保护，防止误删、误改与意外暴露。

### 1.7 修改安全设置
- 在任何时候可通过「密保及密码修改」重新验证主密码后，选择：
  - **只修改主密码**（新密码不能与旧密码相同）；
  - **只修改密保**（自动生成新 Recovery Code）；
  - 两者同时修改。

### 1.8 弱密码与风险扫描
- 侧边栏「安全中心」实时统计：
  - **弱密码**数量（强度评分 < 40）；
  - **重复密码**数量（同一密码被多条记录使用）；
  - 每条列表项右上角直接显示强度评分，弱密码/临期密码有 ⚠ 标记。

---

## 二、密码管理

### 2.1 密码条目字段
每条密码记录支持丰富的字段：

| 字段 | 说明 |
|---|---|
| 账号类型 | 平台 / 网站 / 系统 / APP / 其他，可自定义类型 |
| 名称 | 标题（默认「新密码」，保存时为空自动命名「未命名」） |
| 账号 / 用户名 | 登录账号 |
| 密码 | 支持明文切换显示（👁） |
| 邮箱 | 绑定邮箱 |
| 手机号 | 绑定手机号 |
| 平台昵称 | 平台显示昵称 |
| 网址 / IP / APP 名称 | 支持一键在浏览器打开（HTTP/HTTPS/IP/域名自动识别） |
| 分类 | 归属分类 |
| 标签 | 多个标签，支持常用标签快捷点选 |
| 收藏 | ★ 收藏标记 |
| 密码过期设置 | 永不过期 / 30 天 / 90 天 / 180 天 / 自定义日期 |
| 备注 | 自由文本 |

### 2.2 密码生成器
- 内置**强密码生成器**：一键生成 24 位随机密码，字符集避开易混淆字符（无 0/O、1/l/I 等），并混入 `!@#$%^&*` 特殊字符。

### 2.3 密码强度与过期提醒
- 实时**密码强度分级**：未设置 / 弱 / 中等 / 强 / 超强；
- **7 天内即将过期**的条目在列表中**红色边框高亮**并显示 ⚠ 标记；
- 到期提醒在详情「时间信息」区块中明确显示剩余天数。

### 2.4 查看与编辑模式
- 详情区默认**只读查看模式**，浏览不会触发数据变更；
- 点击「✎ 编辑密码」进入编辑模式，修改后需点击「保存」；
- 存在未保存修改时，切换密码/锁定/离开会弹出「保存 / 放弃 / 继续编辑」确认框，防止误丢数据。

### 2.5 修改记录（历史）
- 每条密码保留**最近 3 次修改记录**；
- 记录内容**只标记哪些字段发生变化**（如「密码、备注」），**不保存字段的具体新旧值**，兼顾可追溯与隐私。

### 2.6 收藏
- 列表点击 ☆/★ 或右键菜单快速收藏/取消收藏；
- 侧边栏「★ 收藏」视图快速筛选。

### 2.7 排序
- **拖拽排序**：按住条目拖动调整顺序（搜索结果视图下不允许排序）；
- 右键菜单支持**上移 / 下移 / 置顶**；
- 顺序自动重编号（`#1 #2 #3 ...`）。

### 2.8 搜索
- 顶部搜索框实时过滤，匹配**名称、账号、邮箱、手机号、昵称、网址、分类、标签、类型**。

### 2.9 回收站与批量操作
- 删除的密码进入**回收站**，按设置**自动保留 7 / 14 / 21 / 30 天**后清理；
- 回收站支持**恢复**、**单条彻底删除**、**清空回收站**；
- 列表支持**多选（全选）→ 批量删除**。

---

## 三、分类管理

### 3.1 多级分类
- 支持**父子层级**分类，可无限层级展开；
- 内置默认分类：默认、工作、财务、社交、开发、其他（各带图标）。

### 3.2 分类操作
- **新建分类 / 新建子分类**（右键分类）；
- **编辑分类**：改名、改图标（emoji）、调整父级（可防止环状与移动到自身子级）；
- **删除分类**：其条目自动归入「默认」分类，子分类上提为父级同级；
- **展开 / 收起**：双击或点击箭头 ▸/▾；
- **拖拽排序**与**上移 / 下移 / 置顶**；
- 侧边栏显示每个分类下的条目计数（含子分类）。

---

## 四、数据管理

### 4.1 完整备份（.vault）
- 一键生成**完整加密备份**（包含密码条目 + 分类），文件后缀 `.vault`；
- 备份前必须**再次验证当前主密码**；
- 备份文件仍是加密 Vault，不包含明文主密码；
- 备份过程自动做**解密校验**，验证通过才落盘。

### 4.2 自动版本备份
- 开启后，每次 Vault **成功保存**后自动向**独立目录**写入一份加密副本；
- **内容变化检测**：只有数据发生变化才生成新快照（变化检测同时覆盖密码条目与分类）；
- **保留策略**：可保留 1 / 3 / 5 / 10 / 20 / 50 份，自动清理最旧版本；
- 每次备份都经过解密验证，失败不产生半成品文件；
- 备份目录**必须位于 LocalVault 数据目录之外**（抵御勒索软件建议使用不同磁盘 / 断连外置盘 / 带版本历史的同步目录）；
- 支持「立即备份」手动触发一次；
- 界面显示最近备份时间与失败原因。

### 4.3 账号密码导出（.lvx）
- 仅导出密码条目为 `.lvx` 加密文件，用于**跨电脑迁移**；
- 导出需验证当前 Vault 主密码；
- 文件格式带魔数 `LVX2`，结构 = 盐 + 包裹密钥 + 加密载荷。

### 4.4 账号密码导入（.lvx）
- 导入另一台电脑导出的 `.lvx`，需输入**原电脑主密码**解密验证；
- 严格拒绝明文文件导入；旧版 `LVX1` 导出需在原电脑重新导出；
- **冲突处理**：按「类型 + 用户名」判断重复，可逐条选择**跳过 / 替换**；
- 缺失的分类自动创建。

### 4.5 批量导入（CSV 模板）
- 内置**中文 CSV 模板**下载，可直接在 Excel / WPS 中批量填写；
- 支持列：名称、账号/用户名、密码、网址/IP/APP、类型、分类、邮箱、手机号、昵称、标签、收藏、密码有效期、备注；
- **自动识别并忽略模板示例数据**，不会导入演示行；
- 编码兼容 **UTF-8（含 BOM）、UTF-16（LE/BE）、GBK/GB18030**；
- 限制保护：单字段 ≤ 4096 字符、密码 ≤ 2048、总行数 ≤ 10 万、文件 ≤ 50 MB；
- 导入前可预览全部记录，重复项按「类型 + 用户名」提示跳过/替换；
- 导入后**建议立即删除明文 CSV**。

### 4.6 从备份恢复
- 导入 `.vault` 完整备份前，先用主密码验证备份；
- 验证成功后**预览**备份内容（条目数、分类数），再选择：
  - **合并到当前 Vault**：不删除当前数据，追加备份中的新条目，补齐缺失分类；
  - **替换当前 Vault**：验证通过后才替换，Windows 下替换失败自动回滚保留原 Vault。

---

## 五、系统功能

### 5.1 软件更新
- 手动点击「软件更新」才联网检查 GitHub Releases；
- 通过 **Tauri Updater 签名校验**，更新包必须签名有效才会安装；
- 展示版本说明、发布时间、下载进度；
- 不包含任何密码数据上传。

### 5.2 关于信息
- 展示版本号、数据库类型（SQLite）、KDF（Argon2id）、AEAD（XChaCha20-Poly1305）。

### 5.3 界面定制
- 左侧分类栏宽度、中间列表宽度均可**拖动调节**并记忆；
- 密码列表宽度可调，窄屏自适应。

---

## 六、发布形态

### 6.1 安装版
- 通过 NSIS / MSI 安装包安装，Vault 数据存放于**当前用户本地数据目录**。

### 6.2 绿色便携版
- 解压即用，数据存放在**程序目录下 `data\`**；
- 通过 `portable.flag` 标记切换存储位置；
- 适合放入 U 盘 / 移动硬盘随身携带（需额外做好加密备份）；
- 依赖系统 WebView2 Runtime。

---

## 七、功能速查表

| 需求 | 入口 |
|---|---|
| 新建密码 | 侧边栏「＋ 新建密码」/ 空白处右键 |
| 查找密码 | 顶部搜索框 |
| 分类管理 | 左侧分类栏右键 |
| 收藏/取消收藏 | 条目 ☆/★ 或右键菜单 |
| 批量删除 | 列表多选 → 批量删除 |
| 恢复删除 | 侧边栏「♻ 回收站」 |
| 修改记录 | 详情页「查看最近 3 次修改记录」 |
| 完整备份 | 数据管理 → 完整备份 |
| 自动备份 | 数据管理 → 安全备份设置 |
| 跨电脑迁移 | 仅导出账号密码 / 导入账号密码 |
| 批量导入 | 数据管理 → 批量导入模板 |
| 改主密码/密保 | 系统 → 密保及密码修改 |
| 找回主密码 | 解锁页 → 找回主密码 |
| 软件更新 | 系统 → 软件更新 |
| 便携版 | 官方 Release 的 Portable ZIP |
