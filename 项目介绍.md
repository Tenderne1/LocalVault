# LocalVault 项目介绍

> 本文档基于 GitHub 仓库 
>
> `Tenderne1/LocalVault`
>
> （主分支，版本 1.7.8）源码整理。



***

## 一、项目概述

**LocalVault** 是一款 **本地优先（Local-first）的桌面密码管理器**，采用「加密存储 + 离线可用」的设计理念：所有密码数据加密后仅保存在用户自己的电脑上，主密码和数据密钥从不离开本机，也不依赖任何云服务或在线账号。

它面向希望把账号密码**完全掌控在自己手中**的用户，适合个人日常凭据管理、企业内部大量设备 / 系统账号的集中维护，以及需要高安全边界的场景。



* **当前版本**：1.7.8

* **默认语言**：中文

* **运行平台**：Windows（Tauri 桌面应用）

* **仓库地址**：[https://github.com/Tenderne1/LocalVault](https://github.com/Tenderne1/LocalVault)



***

## 二、设计理念与定位



| 维度         | 说明                                                    |
| ---------- | ----------------------------------------------------- |
| **本地优先**   | 密码数据库（`vault.db`）存放在本机，离线可用；联网仅用于用户主动触发的「软件更新检查」      |
| **零信任云端**  | 不上传、不同步任何密码数据，无账号体系，无遥测                               |
| **强加密默认**  | 采用 Argon2id 密钥派生 + XChaCha20-Poly1305 认证加密，密钥在内存中即时清零 |
| **防误删设计**  | 内置回收站、自动版本备份、备份验证、Windows 文件隐藏 / 只读保护等容灾能力            |
| **面向中文用户** | 界面、错误提示、帮助文本均为中文                                      |



***

## 三、技术栈



| 层    | 技术                                | 说明                                         |
| ---- | --------------------------------- | ------------------------------------------ |
| 桌面框架 | **Tauri 2**                       | 轻量级桌面壳，打包体积小、内存占用低                         |
| 前端   | **React 19 + TypeScript**         | 严格模式（`strict: true`），单文件应用（`src/main.tsx`） |
| 构建工具 | **Vite 7**                        | 开发端口 1420，严格端口模式                           |
| 后端   | **Rust（edition 2021）**            | `VaultManager` 封装全部密码学与存储逻辑                |
| 数据库  | **SQLite（rusqlite，bundled）**      | 加密 Vault 文件本质是 SQLite 数据库                  |
| 加密算法 | **Argon2id + XChaCha20-Poly1305** | KDF + AEAD 认证加密                            |
| 密码安全 | **zeroize**                       | 内存中的密钥在使用后立即归零                             |
| 插件   | dialog / opener / updater         | 文件对话框、打开外部链接、软件更新                          |

**前端依赖**（`package.json`）：



* `@tauri-apps/api`、`@tauri-apps/plugin-dialog`、`@tauri-apps/plugin-updater`、`@tauri-apps/plugin-opener`

* `react` / `react-dom` 19

* 开发依赖：TypeScript 5.9、Vite 7.3、`@vitejs/plugin-react`

**后端依赖**（`src-tauri/Cargo.toml`）：



* `tauri` 2、`tauri-plugin-dialog/opener/updater`

* `argon2` 0.5（KDF）、`chacha20poly1305` 0.10（AEAD）

* `rusqlite` 0.32（SQLite，bundled）

* `rand_core`、`zeroize`、`dirs`、`hex`、`encoding_rs`、`serde`/`serde_json`



***

## 四、项目结构



```
LocalVault/

├── index.html                  # 前端入口 HTML

├── package.json                # 前端依赖与脚本

├── package-lock.json

├── vite.config.ts              # Vite 配置（端口 1420）

├── tsconfig.json               # TypeScript 严格配置

├── CHANGELOG.md                # 版本变更记录（唯一记录入口）

├── README.md                   # 项目说明

├── .gitignore

├── src/                        # 前端源码（React + TS）

│   ├── main.tsx                # 全部 UI 逻辑（视图、弹窗、编辑器）

│   └── styles.css              # 界面样式

├── src-tauri/                  # 桌面后端（Rust）

│   ├── Cargo.toml              # Rust 依赖与元信息

│   ├── build.rs

│   ├── tauri.conf.json         # Tauri 配置（窗口、CSP、更新器、打包）

│   ├── capabilities/

│   │   └── default.json        # 权限声明（dialog/updater/opener 白名单）

│   ├── icons/                  # 应用图标

│   └── src/

│       ├── main.rs             # Tauri 入口 + 全部 Tauri command 注册

│       └── vault.rs            # 核心：加密、存储、备份、恢复、导入导出

├── scripts/

│   ├── build-windows.ps1       # Windows 一键构建（安装包 + 便携版）

│   └── build-portable.ps1      # 绿色便携版打包脚本

└── .github/workflows/

&#x20;   ├── ci.yml                  # CI：Windows 上测试 + 静态检查 + 构建

&#x20;   └── release.yml             # 发布：打 tag 后自动构建并上传 Release
```



***

## 五、安全架构（加密模型）

LocalVault 采用「**主密码派生密钥包裹数据密钥**」的双层加密结构：



```
用户主密码

&#x20;   │  Argon2id（128 MiB 内存、3 次迭代、2 路并行；随机 16 字节盐）

&#x20;   ▼

KEK（密钥加密密钥）

&#x20;   │  XChaCha20-Poly1305 加密包裹

&#x20;   ▼

DEK（数据加密密钥，32 字节随机）

&#x20;   │  XChaCha20-Poly1305（每条记录独立随机 nonce + AAD 绑定）

&#x20;   ▼

Vault（SQLite：entries / categories / history / trash / meta）
```



* **主密码永不落盘**，只用于在内存中派生 KEK。

* **每条密码条目**加密时绑定 `LocalVault|entry|<id>|v1` 作为附加认证数据（AAD），防止密文被替换 / 搬移。

* **历史记录、恢复密钥**同样使用独立的 AAD 上下文加密。

* **密钥零化**：DEK 在锁定、进程退出（`Drop`）时通过 `zeroize` 清零。

* **文件保护**：Windows 下自动为 Vault 数据目录设置「隐藏 + 只读」属性；每次启动视为锁定状态并恢复保护。

* **CSP 安全策略**：`default-src 'self'`，前端仅允许本地 IPC 通信。

**加密参数（固定常量）**：



| 参数           | 值                   |
| ------------ | ------------------- |
| 加密格式版本       | 2（兼容读取 v1 备份）       |
| 盐长度          | 16 字节               |
| DEK 长度       | 32 字节               |
| Nonce 长度     | 24 字节               |
| Argon2id 内存  | 131072 KiB（128 MiB） |
| Argon2id 迭代  | 3                   |
| Argon2id 并行度 | 2                   |
| 主密码最短长度      | 8 位                 |



***

## 六、核心能力一览



* 主密码 + Recovery Code + 本地密保三合一恢复体系

* 多级分类（父子层级、展开 / 收起、拖拽排序）

* 密码强度评分、弱密码 / 重复密码扫描、7 天内过期提醒

* 密码生成器、复制后 30 秒自动清空剪贴板

* 收藏、标签、回收站（自动保留 7–30 天）、批量删除

* 每条密码最近 3 次修改记录（仅记录字段名，不保存具体内容）

* 完整 `.vault` 加密备份、账号密码 `.lvx` 跨电脑导入 / 导出

* 自动版本备份（独立目录、内容变化检测、保留策略 1–50 份）

* CSV 批量导入模板（UTF-8 / UTF-16 / GBK/GB18030 编码）

* 自动锁定（1 / 5 / 10 / 30 分钟可选）

* 手动检查软件更新（Tauri Updater 签名校验）

* 安装版 / 绿色便携版两种形态



***

## 七、构建与发布

### 本地开发



```
npm install

npm.cmd run tauri:dev        # 启动开发模式（Vite 1420 + Tauri）
```

### Windows 构建



```
npm.cmd run tauri:build      # 生成 NSIS / MSI 安装包

npm.cmd run build:portable   # 生成绿色便携版（release\LocalVault-Portable-x64）
```

一键脚本：`npm.cmd run build:windows`（自动完成依赖安装 → 安装包 → 便携版）。

### CI/CD（GitHub Actions）



* **CI（ci.yml）**：每次 push / PR 触发，在 `windows-latest` 上执行 `cargo test`、`cargo clippy -D warnings`、`npm run build`。

* **发布（release.yml）**：推送 `v*` 标签时自动触发：


  * 构建 Windows 安装包并发布到 GitHub Releases（含 updater 签名产物）；

  * 生成便携版 ZIP；

  * 生成 `SHA256SUMS.txt` 校验和文件并一并上传。

### 更新机制



* 更新器签名公钥内置于 `tauri.conf.json`；

* 更新端点：GitHub Releases 的 `latest.json`；

* 更新包必须通过 Tauri Updater 签名验证才会安装，且**仅在用户主动点击「软件更新」时联网**。



***

## 八、数据与安全边界



* **Vault 位置**：安装版位于当前用户本地数据目录下的 `LocalVault\`；便携版位于程序目录的 `data\`。

* **备份位置要求**：完整备份与自动版本备份**必须**位于 LocalVault 数据目录之外（用于抵御勒索软件同目录加密 / 删除）。

* **明文中转**：CSV 批量导入模板是明文文件，导入完成后应及时删除或妥善保管。

* **隐私**：不收集任何遥测；更新请求中不含任何密码数据；Recovery Code 仅本地保存。



***

## 九、版本历史

当前版本 **1.7.8**（2026-09-02）主要变化：



* 修复导入旧版（1.6.3）`.vault` 完整备份时分类表缺少 `parent_name` 导致验证失败的问题，旧版分类按一级分类兼容读取。

* 完整备份预览、合并兼容旧版分类表结构，读取备份时不修改原始备份文件。

* 完整 Vault 恢复增加 Windows 文件替换失败保护，验证成功后才替换，失败时保留原 Vault。

* 自动版本备份的变化检测同时覆盖密码条目与分类数据（分类新增 / 修改 / 删除 / 排序变化也能触发新快照）。

* 清理未使用的前端对话框组件、常量、样式及 Rust 辅助代码，移除未使用依赖。

> 版本变更明细以仓库 
>
> `CHANGELOG.md`
>
>  为唯一记录入口。