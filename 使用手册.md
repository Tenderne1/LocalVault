# LocalVault 使用手册

> 适用版本：1.7.9 · 运行平台：Windows
> 本手册按「安装 → 首次设置 → 日常使用 → 安全 → 备份迁移 → 更新 → 常见问题」逐步说明。

---

## 目录

1. [安装与启动](#一安装与启动)
2. [首次使用：创建 Vault](#二首次使用创建-vault)
3. [每日使用](#三每日使用)
4. [分类管理](#四分类管理)
5. [安全功能](#五安全功能)
6. [忘记主密码怎么办](#六忘记主密码怎么办)
7. [数据备份与迁移](#七数据备份与迁移)
8. [批量导入 CSV](#八批量导入-csv)
9. [软件更新](#九软件更新)
10. [常见问题（FAQ）](#十常见问题faq)

---

## 一、安装与启动

### 方式 1：安装版（推荐）

1. 前往项目 Release 页面下载 Windows 安装包（NSIS `.exe` 或 MSI `.msi`）；
2. 双击运行，按提示完成安装；
3. 从开始菜单或桌面快捷方式启动 **LocalVault**。

> 安装版的数据存放在**当前 Windows 用户本地数据目录**下的 `LocalVault` 文件夹（路径形如 `C:\Users\<用户名>\AppData\Local\LocalVault`）。

### 方式 2：绿色便携版

1. 下载 `LocalVault-Portable-x64.zip`；
2. 解压得到 `LocalVault-Portable-x64` 文件夹（**整个文件夹一起保存，不要只移动 exe**）；
3. 双击 `LocalVault.exe` 即可运行。

便携版注意事项：

- Vault 数据保存在**程序目录下 `data\vault.db`**；
- **不要删除 `portable.flag`**，否则程序会恢复使用 Windows 用户数据目录；
- 依赖系统 **Microsoft Edge WebView2 Runtime**，若无法启动请先安装 WebView2；
- 放入 U 盘/移动硬盘随身携带时，**务必额外做好加密备份**（见第七章）。

---

## 二、首次使用：创建 Vault

首次启动会进入「创建你的 Vault」页面。

### 第 1 步：设置主密码

- 输入**主密码**并**再次确认**；
- 主密码规则：**至少 8 位**，且同时包含**数字 + 大写字母 + 小写字母 + 特殊符号**（如 `Abc@2026!`）；
- 页面下方有**强度评分条**，建议评分达到较高水平再继续。

> ⚠️ 主密码是解开所有数据的唯一钥匙，**无法找回**。请务必牢记，并配合下面的恢复方式使用。

### 第 2 步：设置恢复方式

创建后会生成一组 **Recovery Code**（如 `K7J2-QP9R-...`），并让你设置**三组密保问答**：

1. 先点击「复制 Recovery Code」，**离线妥善保存**（纸质或加密笔记，切勿只存在电脑明文里）；
2. 为三个默认问题填写**答案**（答案不能为空，可用中文，请填只有你自己知道的内容）；
3. 点击「保存恢复设置并进入」。

> 如果暂时不想设置，可点「稍后设置」，之后仍可随时在「系统 → 密保及密码修改」中补齐。

### 第 3 步：进入主界面

完成以上步骤后即进入主界面，可开始添加密码。

---

## 三、每日使用

### 3.1 解锁 / 锁定

- **解锁**：启动后输入主密码，点击「解锁」（回车亦可）；
- **手动锁定**：点击顶部「🔒 锁定」按钮，立即回到解锁页并清空内存；
- **自动锁定**：在左侧「安全中心」选择空闲自动锁定时间（1 / 5 / 10 / 30 分钟），超时无操作自动锁定。

### 3.2 添加一条密码

1. 点击侧边栏「＋ 新建密码」（或在列表空白处右键 → 添加密码）；
2. 右侧进入编辑模式，填写：
   - **名称**（必填，如 `GitHub`）
   - **账号/用户名**、**密码**（可点「生成」让系统生成强密码）
   - 邮箱、手机号、平台昵称、网址/IP/APP、分类、标签、备注等（可选）
3. 点击「保存」完成。

> 新建但未填写任何内容就离开时，会自动丢弃空条目，不会产生垃圾数据。

### 3.3 查看 / 编辑 / 删除

- **查看**：点击列表中的条目，右侧以只读模式展示详情；
- **编辑**：点击「✎ 编辑密码」→ 修改 → 「保存」；
- **删除**：点击「删除」，或在列表/右键菜单选择删除 → 确认永久删除。删除的条目进入回收站（不是立即消失）。

> 有未保存修改时切换条目会弹窗询问「保存 / 放弃 / 继续编辑」，请按需选择。

### 3.4 复制与打开

- **复制密码**：详情页点「复制」，30 秒后剪贴板自动清空；
- **打开网址**：详情页网址栏右侧「↗ 打开浏览器」，自动识别 HTTP/HTTPS/IP/域名。

### 3.5 搜索与筛选

- 顶部搜索框实时搜索：名称、账号、邮箱、手机号、昵称、网址、分类、标签、类型；
- 左侧点击「全部」「★ 收藏」或任意分类即可筛选。

### 3.6 收藏 / 排序

- 列表项 ★ 按钮或右键菜单可收藏/取消；
- 按住条目 **⋮⋮ 手柄拖动**可排序；右键菜单有上移/下移/置顶。

### 3.7 修改记录

- 编辑某条密码后，详情页底部「查看最近 3 次修改记录」可看到该条目最近 3 次被改动的**字段名**（不显示具体内容）。

---

## 四、分类管理

### 4.1 新建分类

1. 点击左侧「＋ 新建分类」；
2. 输入名称（≤ 40 字）、选择**父分类**（可留空为顶级）、选择**图标**（emoji，或从快捷图标中选）；
3. 创建。

### 4.2 子分类

- 在分类上右键 →「＋ 新建子分类」，即创建该分类下的子分类；
- 编辑分类时可修改父级，实现层级调整；
- 禁止把分类移到自己的子分类下（系统会拦截）。

### 4.3 展开 / 收起 / 排序

- 双击有子分类的分类，或点击箭头 ▸/▾，可展开/收起；
- 按住分类拖拽、或右键上移/下移/置顶，可调整同级顺序；
- 侧边栏显示每个分类的条目数（含子分类）。

### 4.4 删除分类

- 右键分类 →「删除分类」；
- 删除后该分类下的条目自动归入「默认」分类，其子分类上提为父级同级；
- 「默认」分类不可删除、不可重命名、不可设为子分类。

---

## 五、安全功能

### 5.1 主密码防暴力破解
LocalVault 会在解锁阶段对连续错误进行限制：

- **10 分钟滚动窗口**统计主密码失败次数；
- 连续失败达到 **3 次**后，需要输入 6 位随机图片验证码；
- 验证码输入错误后，**旧验证码立即失效并自动刷新**，输入框也会清空；
- 已进入验证码阶段时，即使验证码正确但**主密码错误**，该次失败仍会计入失败次数，并立即刷新验证码；
- 连续失败 **5 / 10 / 15 / 20 次**分别锁定 **30 秒 / 5 分钟 / 30 分钟 / 1 小时**；
- 应用重新启动不会简单清除已经记录的失败次数和有效锁定时间；
- 主密码成功解锁后，失败次数、锁定状态和验证码状态会清零。

> 验证码和失败次数的关键检查由 Rust 后端执行，不能仅通过绕过前端界面来取消限制。

### 5.2 锁定期间仍可找回主密码
主密码被临时锁定时，**「找回主密码」仍然可以正常使用**：

1. 在解锁页点击「找回主密码」；
2. 输入 Recovery Code；
3. 回答三组密保问题；
4. 通过身份验证后设置新的主密码；
5. 设置成功后，原来的主密码失败次数、锁定状态和验证码状态会被清除；
6. 返回解锁页后直接使用新主密码解锁。

> 主密码防暴力破解只限制「主密码尝试」，不会锁死 Recovery 恢复入口。Recovery 验证成功后也不会被之前的锁定状态继续阻挡。

### 5.3 修改主密码 / 密保
1. 「系统 → 密保及密码修改」；
2. 先输入**当前主密码**验证身份；
3. 勾选要修改的项目：
   - **修改主密码**：输入并确认新主密码（不能与旧密码相同）；
   - **修改密保**：填写三组新问答；保存后会**自动生成新的 Recovery Code**，并尝试自动复制，旧码立即失效；
4. 点击「保存修改」。

### 5.4 查看风险统计

### 5.2 查看风险统计

- 左侧「安全中心」显示：**弱密码**（评分 < 40）数量、**重复密码**数量；
- 列表条目右上角的数字是密码强度评分，⚠ 标记弱密码或 7 天内过期。

### 5.3 密码过期提醒

- 编辑密码时在「时间信息」设置过期策略（永不过期 / 30 / 90 / 180 天 / 自定义日期）；
- 距离过期 ≤ 7 天时，列表条目**红色边框高亮**并显示 ⚠。

---

## 六、忘记主密码怎么办
前提：创建时已设置 Recovery Code + 三组密保答案。

1. 在解锁页点击「**找回主密码**」；
2. 输入 **Recovery Code**；
3. 回答 **三组密保问题**；
4. 点击「验证身份」；
5. 验证通过后进入「设置新主密码」页面，设置符合规则的新主密码；
6. 完成后回到解锁页，**使用新主密码解锁**。

### 6.1 主密码被锁定时怎么办
如果因为连续输入错误主密码而进入 30 秒、5 分钟、30 分钟或 1 小时锁定，**无需等待锁定结束**即可点击「找回主密码」。

Recovery 流程独立于主密码暴力破解锁定：

- 锁定期间「找回主密码」按钮仍可用；
- Recovery Code + 三组密保验证不计入主密码失败次数；
- Recovery 验证成功并设置新主密码后，会自动清除旧的失败次数、锁定状态和验证码；
- 随后可以直接用新主密码重新解锁。

> 若既忘记主密码，又没有 Recovery Code / 密保答案，**数据将无法恢复**（这是加密设计的必然结果，也是保护数据不被他人盗取的代价）。

## 七、数据备份与迁移

### 7.1 完整备份（.vault）

完整备份包含**全部密码条目 + 分类**，是最高级别的备份。

1. 「数据管理 → 完整备份」；
2. 选择保存位置（**不要选 LocalVault 数据目录内部**）与文件名（`.vault`）；
3. 输入并确认**当前主密码**；
4. 生成成功后有提示，文件为加密格式。

### 7.2 自动版本备份（推荐开启）

1. 「数据管理 → 安全备份设置」；
2. 勾选「启用自动版本备份」；
3. 点击「选择文件夹」指定**独立备份目录**（必须位于 LocalVault 数据目录之外，建议不同磁盘/外置盘/带版本历史的网盘目录）；
4. 选择保留版本数（1–50 份）；
5. 点击「保存并验证」——会立即做一次备份验证。

之后每次保存 Vault，系统会**自动**检测内容变化并在备份目录生成新的加密快照（条目和分类变化都会触发），并自动清理超出保留数的旧版本。

- 侧边栏「数据管理」下方会显示「✓ 自动备份已启用」或失败原因；
- 需要时点击「立即备份」手动触发一次。

### 7.3 跨电脑迁移：仅导出账号密码（.lvx）

1. 旧电脑：「数据管理 → 仅导出账号密码」；
2. 选择保存位置（`.lvx`），输入当前主密码确认；
3. 新电脑：「数据管理 → 导入账号密码」；
4. 选择该 `.lvx` 文件，输入**原电脑主密码**；
5. 预览导入记录，重复项（按「类型 + 用户名」判断）可选择**跳过 / 替换**；
6. 「应用导入」完成。

> `.lvx` 是加密文件；导入过程只在本机解密处理，不会保存源主密码。

### 7.4 从完整备份恢复

1. 在解锁页点「恢复已有 Vault」，或在主界面「数据管理 → 导入完整备份」；
2. 选择 `.vault` 文件；
3. 输入**该备份对应的主密码**进行验证；
4. 验证成功后预览备份内容（条目数、分类数）；
5. 选择恢复方式：
   - **合并到当前 Vault**：追加备份中新条目、补齐缺失分类，**不删除**当前已有数据；
   - **替换当前 Vault**：用备份整体覆盖当前数据（替换前自动做旧 Vault 保护，失败会回滚）。

---

## 八、批量导入 CSV

适用场景：一次性录入大量设备、系统、平台的账号密码。

### 第 1 步：下载模板

「数据管理 → 批量导入模板」→ 点击「**下载模板**」，得到一个 `LocalVault-批量导入模板.csv`。

模板自带 2 条演示数据（`示例：GitHub` / `示例：企业邮箱`），**系统会自动识别并忽略**，也可直接删除示例行。

### 第 2 步：填写数据

用 Excel / WPS 打开模板，按列填写：

| 列 | 必填 | 说明 |
|---|---|---|
| 名称 | ✅ | 记录名称（≤ 200 字） |
| 账号/用户名 | ✅ | 登录账号 |
| 密码 | ✅ | 密码（≤ 2048 字，不能为空） |
| 网址/IP/APP名称 | 选填 | 网址或 IP 或 App 名称 |
| 账号类型 | 选填 | 平台/网站/系统/APP/其他，默认「网站」 |
| 分类 | 选填 | 默认「默认」，不存在的分类会自动创建 |
| 邮箱 / 手机号 / 平台昵称 | 选填 | 绑定信息 |
| 标签 | 选填 | 用顿号/逗号分隔多个标签 |
| 收藏 | 选填 | `是/否`、`1/0`、`true/false` 等 |
| 密码有效期 | 选填 | 仅支持「永不过期」「30天」「90天」「180天」 |
| 备注 | 选填 | 自由文本 |

### 第 3 步：导入

1. 「数据管理 → 批量导入模板」→「**从模板批量导入**」；
2. 选择填好的 CSV 文件；
3. 系统解析并预览，可逐条处理重复项（跳过/替换）；
4. 「应用导入」完成。

### 编码支持

自动识别 **UTF-8（含 BOM）、UTF-16LE/BE、GBK/GB18030**，Excel/WPS 直接另存的中文 CSV 一般可直接导入。

### ⚠️ 安全提示

- CSV 是**明文文件**，不会被加密；
- 模板示例数据不会导入 Vault；
- **导入成功后请立即删除或妥善保管原 CSV 文件**。

---

## 九、软件更新

1. 「系统 → 软件更新」；
2. 点击「检查更新」（此时才联网，仅访问 GitHub Releases，不含任何密码数据）；
3. 有新版时查看版本说明，点击「下载并安装」；
4. 下载过程显示进度，安装包通过 **Tauri Updater 签名校验**后才允许安装。

> 更新为**被动模式（passive）**安装，一般无需手动干预。

---

## 十、常见问题（FAQ）

**Q1：数据存在哪里？**
安装版在 `C:\Users\<用户名>\AppData\Local\LocalVault\vault.db`；便携版在程序目录 `data\vault.db`。整个目录在 Windows 下会被设为隐藏 + 只读保护。

**Q2：换电脑 / 重装系统怎么迁移？**
用「仅导出账号密码（.lvx）」+「导入账号密码」迁移条目；如需连分类一起迁移，用「完整备份（.vault）」+「导入完整备份」恢复。

**Q3：备份目录有什么限制？**
必须位于 LocalVault 数据目录**之外**，否则会被系统拒绝（这是为了防止勒索软件把数据与备份同目录一起加密）。

**Q4：复制密码后为什么剪贴板会自动清空？**
这是刻意设计的安全功能：密码复制后 30 秒自动清空剪贴板。

**Q5：导入 `.lvx` 提示“旧版 LVX1 导出文件”？**
1.6.x 之前的旧格式导出不支持跨 Vault 导入，请到原电脑用新版重新导出一次。

**Q6：CSV 导入报“编码无法识别”？**
请确保文件是 UTF-8 / UTF-16 / GBK/GB18030 编码；Excel 中文版默认 GBK 一般可直接识别。

**Q7：便携版双击无法启动？**
需要系统装有 **Microsoft Edge WebView2 Runtime**；未安装时安装后即可使用。

**Q8：删除的密码还能找回吗？**
可以，只要在回收站自动清理期（默认 30 天，可设 7/14/21 天）内，进入「♻ 回收站」点击「恢复」即可。

**Q9：自动备份失败怎么办？**
「数据管理」区域会显示失败原因（常见：备份目录不可写、被移除、磁盘断开）。检查备份目录是否存在且可写，重新保存一次数据即可重试；也可以先点「立即备份」验证。

**Q10：修改密保后旧 Recovery Code 还能用吗？**
不能。修改密保会立即作废旧码并生成新码，请保存新的 Recovery Code。

**Q11：为什么验证码输入错误后会变成另一张？**
这是刻意设计的安全机制。验证码提交失败后，旧验证码立即失效，系统自动生成新的验证码，避免重复尝试同一个验证码。

**Q12：验证码正确但主密码错误，为什么验证码也会刷新？**
验证码只负责证明当前提交者完成了额外挑战；主密码仍然必须正确。为了避免同一个验证码被重复使用，主密码验证失败后也会立即轮换验证码。

**Q13：主密码锁定后整个界面不能操作吗？**
不会。主密码输入、验证码提交等解锁操作会受到锁定限制，但「找回主密码」仍然可用。通过 Recovery 成功设置新主密码后，可以立即恢复正常解锁。


---

> 若遇到本文档未覆盖的问题，可参考仓库 `README.md` 与 `CHANGELOG.md`，或查看源码中的错误提示（界面上的错误信息即为具体原因）。
